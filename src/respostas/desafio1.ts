// Como podemos rodar isso em um arquivo .ts sem causar erros? (Desafio 1)
interface IEmployee {
    code: number,
    name: string,
}

let funcionario: IEmployee = {
    code: 10,
    name: 'John',
}
